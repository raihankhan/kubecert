apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubecert-controller.fullname" . }}-test-connection"
  labels:
    {{- include "kubecert-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubecert-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
